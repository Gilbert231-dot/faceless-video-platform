*.mp4
# The landing-page demo clip is a deliberate, always-wanted repo asset
# (regenerated by make_landing_demo.py on every run) — un-ignore it.
!assets/demo/real_demo.mp4
*.mp3
# Sound effects for video pipeline — needed by GitHub Actions
!assets/sound_effects/ding.mp3
!assets/sound_effects/whoosh.mp3
*.srt
*.png
!assets/storylab_avatar.png
!favicon-16x16.png
!favicon-32x32.png
!favicon-96x96.png
!apple-touch-icon.png
!android-chrome-192x192.png
!android-chrome-512x512.png
!icon-512.png
*.txt
www.youtube.com_cookies.txt
# Browser cookies export for the Reddit story refill (Option B) — never commit
reddit_cookies.txt
venv/
__pycache__/
cached_videos/
.env
# Throwaway self-signed cert for the Facebook OAuth setup script
fb_local_certs/
# Debug-mode story copies / test state — never commit these
reddit_stories_debug/
test_used_ids.json

# Local upload config (contains secrets - never commit)
local_config.json
local_uploaded_ids.json
local_downloads/
